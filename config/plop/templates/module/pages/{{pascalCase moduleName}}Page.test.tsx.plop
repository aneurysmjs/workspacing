import { render } from '@testing-library/react';

import {{suffixMe (pascalCase moduleName) 'Page'}} from './{{pascalCase moduleName}}Page';

describe('{{pascalCase moduleName}} page', () => {
	it('should have "{{pascalCase moduleName}} page" as textContent', () => {
		const { container } = render(<{{pascalCase moduleName}}Page />);
		const div = container.firstChild as HTMLDivElement;

		expect(div.textContent).toEqual('{{pascalCase moduleName}} page');
	});
});
